<h1>Listing topics</h1>

<% @benmarks.each do |benmark| %>

<% end %>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @benmarks.each do |benmark| %>
      <tr>
        <td><%= benmark.url %></td>
        <td><%= link_to 'Show', benmark %></td>
        <td><%= link_to 'Edit', edit_benmark_path(benmark) %></td>
        <td><%= link_to 'Destroy', benmark, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Benmark', new_benmark_path %>
